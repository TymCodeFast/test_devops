services:
  web:
    build: ./
    ports:
      - "8091:5000"
    environment:
      - DATABASE_URL=postgresql://postgres:example@db:5432/postgres
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: example
    ports:
      - "55433:5432"